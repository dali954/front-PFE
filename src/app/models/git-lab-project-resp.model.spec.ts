import { GitLabProjectResp } from './git-lab-project-resp.model';

describe('GitLabProjectResp', () => {
  it('should create an instance', () => {
    expect(new GitLabProjectResp()).toBeTruthy();
  });
});
